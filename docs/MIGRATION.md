# Migration provenance

This repository is the public canonical AUREUS-2026 implementation.

It was derived from the governed compute-revenue work developed in private repository `jadeldiaz01-png/ai-income-revenue-engine`, particularly the P4-E line. The public repository intentionally does not import private Git objects, credentials, private evidence artifacts or unrelated legacy verticals.

The canonical AUREUS implementation is self-contained under `src/aureus`, `config`, `infra`, `sql`, `tests` and `.github/workflows`.
